# Rust build output
/target

# Node deps (tools/gbd-download helper)
node_modules

# macOS
.DS_Store

# Mortality data and intermediates
# mortality_data.csv is a ~38MB file embedded via include_str! in src/lib.rs.
# It is recovered from the committed wasm binary and must stay out of git.
data
mortality_data.csv
combine
combined.csv

# TODO: ignore pkg/ once the data table is small enough to build in CI.
# The live site currently serves pkg/ straight from the repo, so it stays
# tracked for now. Revisit after the GBD data rewrite (pre-aggregated table).
# pkg/
